platform: linux 
image_resource:
  type: docker-image
  source: {repository: springio/java, tag: 'docker'}
inputs:
  - name: source
run:
  dir: source/complete
  path: ./gradlew
  args: [build]
params:
  TERM: -dumb
  GRADLE_OPTS: -Dorg.gradle.native=false