platform: linux 
image_resource:
  type: docker-image
  source: {repository: java, tag: '8'}
inputs:
  - name: source
run:
  dir: source/complete
  path: ./gradlew
  args: [build]
params:
  TERM: -dumb
  GRADLE_OPTS: -Dorg.gradle.native=false