platform: linux 
image_resource:
  type: docker-image
  source: {repository: springio/java, tag: 'docker'}
inputs:
  - name: target
outputs:
  - name: maven_home
run:
  path: docker
  args: ["volume", "ls"]
params:
  TERM: -dumb
